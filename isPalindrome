def Palindrome(string):
    m=len(string)
    if m==1:
        return 1
    else:
        if string[0]==string[-1]:
            return 1*Palindrome(string[1:int(len(string)-1)])
        return 0


print(Palindrome('GADADAG'))
